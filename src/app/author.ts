export class Author {
	name: string;
	birthday: string;
	birthPlace: string;

	constructor(name: string, birthday: string, birthPlace: string) {
		this.name = name;
		this.birthday = birthday;
		this.birthPlace = birthPlace;
	}
}